[project]
name = "TelegramMessageReader"
description = "My telegram message reader bot"
package-mode = false
authors = [
    {name = "viresh"}
]
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "python-dotenv (>=1.0.1,<2.0.0)",
    "python-telegram-bot (>=21.10,<22.0)",
    "telethon (>=1.40.0,<2.0.0)",
    "loguru (>=0.7.3,<0.8.0)",
    "websockets (>=15.0.1,<16.0.0)",
    "requests (>=2.32.4,<3.0.0)",
    "websocket-client (>=1.8.0,<2.0.0)",
    "pandas (>=2.3.1,<3.0.0)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
package-mode = false